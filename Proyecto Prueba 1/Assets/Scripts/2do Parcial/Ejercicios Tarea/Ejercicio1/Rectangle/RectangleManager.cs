using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RectangleManager : MonoBehaviour
{
	/*
	Rectangle rect;
	Vector2 a;
	Vector2 b;
	
	float aX;
	float aY;
	float bX;
	float bY;
	List<float> values;
	
	void Awake()
	{
		Rectangle rect = new Rectangle(a,b);	
		
	}
	
	void Start()
	{
		List<float> values = new List<float>();
		a = new Vector2 (aX, aY);
		b = new Vector2 (bX, bY);
		rect = new	Rectangle(a, b);
		
	}
	
	public void MakeRectangle()
	{
		Randomize();
		UpdateValues();	
		
	}
	
	void Randomize()
	{
		for (int i = 0; i < values.Count; i++) 
		{
			values[i] = Random.Range(-5f , 5f);
		}

			
	}
	
	void UpdateValues()
	{
		a = new Vector2(values[0], values[1]);
		b = new Vector2(values[2], values[3]);
		
		rect.Height = rect.GetHeight(rect);
		rect.Width = rect.getWidth(rect);
		rect.Area = rect.getArea(rect);
		rect.Perimeter = rect.getPerimeter(rect);
		
	}
	
	public void ShowValues()
	{
		
		Debug.Log("Coordinates are:"+" aX = "+values[0]+" aY = "+values[1]+" bX = "+values[2]+" bY = "+values[3]);
	
		
		Debug.Log("Rectangle Height is: " + rect.Height);
		Debug.Log("Rectangle Base is: " + rect.Width);
		Debug.Log("Rectangle Area is: " + rect.Area);
		Debug.Log("Rectangle Perimeter is; " + rect.Perimeter);
				
	}*/
}
